// import { ADD_COUNT, ADD_TODO } from "./Action";

// const Initial_State = { counter: 0, todo: [] }
// export const Reducer = (store = Initial_State, { type, payload }) => {

//     switch (type) {
//         case ADD_COUNT:
//             return { ...store, counter: store.counter + payload }

//         case ADD_TODO:
//             return { ...store, todo: [...store.todo, payload] }
//         default:
//             return store
//     }

// }

