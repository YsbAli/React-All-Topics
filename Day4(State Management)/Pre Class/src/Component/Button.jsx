export const Button = ({ AddFunc }) => {
  return (
    <>
      <button onClick={AddFunc}>ADD</button>
    </>
  );
};



//here AddFunc is a function that is the key of the props,,, passing as props....

//we can use this props in the button call, use everywhere
